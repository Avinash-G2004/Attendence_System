<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admission Form</title>
    <style>
        /* Reset styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Roboto', sans-serif;
        }

        /* Body background with gradient */
        body {
            background: linear-gradient(135deg, #CFBCF9 , #FFAEF1);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-size: cover;
            font-size: 14px; /* Adjusted the base font size */

            /* background: linear-gradient(to right #CFBCF9 , #FFAEF1); */
        }

        /* Form container */
        .form-container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 350px; /* Smaller container width */
            transform: perspective(1000px) rotateY(10deg);
            animation: formAnimation 1s ease-out forwards;
            border-top-left-radius: 40px;
            /* border-bottom-right-radius: 40px; */
        }

        /* Form animation */
        @keyframes formAnimation {
            0% { opacity: 0; transform: rotateY(45deg); }
            100% { opacity: 1; transform: rotateY(0); }
        }

        h1 {
            text-align: center;
            font-size: 24px;
           color: #2c3e50;
           margin-bottom: 25px;
          font-weight: bold;
          transition: text-decoration-color 0.3s ease;
        }
        h1:hover {
    text-decoration: underline;
    text-decoration-color: #ff6347; /* Set your desired underline color */
}
        form {
            display: flex;
            flex-direction: column;
        }

        /* Input group styles */
        .input-group {
            margin-bottom: 15px; /* Reduced margin */
            position: relative;
        }

        label {
            font-size: 12px; /* Smaller font size */
            color: #444;
            margin-bottom: 5px;
            display: block;
        }

        input {
            width: 100%;
            padding: 8px; /* Reduced padding */
            border: 2px solid #ccc;
            border-radius: 6px; /* Smaller border radius */
            font-size: 14px; /* Smaller input font size */
            background-color: #f1f1f1;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            outline: none;
        }

        input:focus {
            transform: translateY(-3px); /* Slight raise when focused */
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }

        input[type="number"] {
            -moz-appearance: textfield;
        }

        /* Submit Button */
        button {
            padding: 8px 12px; /* Reduced padding */
            border: none;
            background-color: #3b4a52;
            color: white;
            font-size: 14px; /* Smaller font size */
            border-radius: 6px;
            cursor: pointer;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        button:hover {
            background-color: #4e5b64;
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        /* Link Button (Back Button) */
        .back-btn {
            display: inline-block;
            padding: 8px 12px; /* Reduced padding */
            background-color: #4e4376;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            margin-top: 15px;
            text-align: center;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .back-btn:hover {
            background-color: #3b4a52;
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        /* Media Queries for responsiveness */
        @media (max-width: 768px) {
            .form-container {
                width: 90%;
                padding: 20px;
            }

            h1 {
                font-size: 18px; /* Slightly smaller on medium screens */
            }

            input, button {
                font-size: 12px;
            }
        }

        @media (max-width: 480px) {
            .form-container {
                width: 90%;
                padding: 15px;
            }

            h1 {
                font-size: 16px; /* Even smaller for mobile */
            }

            input, button {
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Admission Form</h1>
        <form method="post" action="/addmission">
            <div class="input-group">
                <label for="name">First Name</label>
                <input type="text" name="name" id="name" placeholder="Enter your first name" required>
            </div>
            <div class="input-group">
                <label for="mname">Middle Name</label>
                <input type="text" name="mname" id="mname" placeholder="Enter your middle name" required>
            </div>
            <div class="input-group">
                <label for="lname">Last Name</label>
                <input type="text" name="lname" id="lname" placeholder="Enter your last name" required>
            </div>
            <div class="input-group">
                <label for="branch">Class</label>
                <input type="text" name="branch" id="branch" placeholder="Enter your class (e.g., BCSFY/BCAFY)" required>
            </div>
            <div class="input-group">
                <label for="prn">PRN</label>
                <input type="number" name="prn" id="prn" placeholder="Enter your PRN" required>
            </div>
            <button type="submit">Submit</button>
        </form>
        <a href="/" class="back-btn">Back</a>
    </div>
</body>
</html>
